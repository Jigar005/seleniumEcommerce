package com.apex.customer.service.test;

import java.io.StringWriter;

import javax.xml.bind.JAXBContext;
import javax.xml.bind.JAXBException;
import javax.xml.bind.Marshaller;

import com.apex.customer.service.entity.Customer;

public class XMLParserClient {

	public static String javaObjToXml(int id, String firstName, String lastName, String streetName, String cityName) throws JAXBException {
		Customer customer = new Customer(id, firstName, lastName, streetName, cityName);
		JAXBContext jaxbContext = JAXBContext.newInstance(Customer.class);
		Marshaller marshaller = jaxbContext.createMarshaller();
		marshaller.setProperty(Marshaller.JAXB_FORMATTED_OUTPUT, true);

		StringWriter writer = new StringWriter();
		marshaller.marshal(customer, writer);
		System.out.println(writer);
		String xmlString = writer.toString();
		return xmlString;
	}

}
